# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure
runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
# Add a rewrite to deploy a server-side rendered Next.js app.
# https://firebase.google.com/docs/app-hosting/configure#rewrites
rewrites:
  - source: '/**'
    destination: '/_next/server/server.js'

# Trigger new deploy
# Novo gatilho para deploy
# Final deploy trigger
# Triggering new deploy at Sun Nov 23 2025
# Trigger new deploy after deletion
# Final trigger with correct secret
# Final trigger to solve secret name.
# Final trigger.
# Final trigger to finally resolve the deployment issue.
# The definitive final trigger for deployment.
# One last push to fix this for good.
# This one will work.
# This is the final attempt.
# This will finally work.
# This is the final, final, final attempt that will work.
# This is the absolute final change to fix the deployment.
# Re-triggering deploy after accidental deletion.
# estrategiachinesa 1.0
# estrategiachinesa 1.1
